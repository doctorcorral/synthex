[experiment]
name = "breakout_ranking"
method = "ranking"

[environment]
env = "breakout"
actions = ["right", "noop", "left"]

[synthesis]
depth = 1
max_coeff = 5
n_episodes = 15
top_k = 20
max_iters = 3
cegar_rounds = 3
max_steps = 5000

[validation]
val_episodes = 100
